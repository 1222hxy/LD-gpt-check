# Static frontend public configuration only.
# Do not put secrets here. Values built into Vite must be safe to expose.

# Optional public URL of the API Worker, used only for links/status UI if frontend code needs it.
VITE_PUBLIC_API_BASE_URL="https://codexgo.yhklab.com"
