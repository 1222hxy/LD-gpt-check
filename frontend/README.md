# LD-gpt-check frontend

This is a static project homepage for LD-gpt-check.

Open `index.html` directly in a browser, or serve the folder with any static file server:

```bash
cd frontend
python3 -m http.server 8080
```
